---
name: volna-journal
description: Формат и правила ведения журнала работ над задачей в .volna/journal/TASK-<id>.md — frontmatter состояния, append-only секции по этапам (что/зачем/почему/как/сделано/осталось/нужно), чек-пойнт перед сжатием контекста, извлечение опыта в .volna/knowledge/. Используй при создании журнала, любой записи в него, чек-пойнте и когда нужно восстановить контекст задачи.
---

# Волна: журнал работ

Журнал — **единственный источник правды** о состоянии задачи. Инвариант: по журналу задача
восстанавливается с нуля в новой сессии, без остатков контекста.

## Раскладка

```
.volna/
  state.json                 указатель: {"active":"21571","updated":"…"}   (не коммитится)
  journal/TASK-21571.md      журнал задачи                                 (не коммитится)
  knowledge/INDEX.md         указатель записей по этапам                   (коммитится)
  knowledge/<зона>/<slug>.md одна запись опыта = один файл                 (коммитится)
  project.md                 пути репозиториев, команды, конвенции         (коммитится)
```

`.gitignore` рабочего репозитория: `.volna/state.json`, `.volna/journal/`.
Решение команды — журналы локальные, шарится только `knowledge/`.

## Frontmatter = состояние

```yaml
---
task: 21571
title: "Авто-клапан не рисуется на продакшн-виде при зеркале"
type: bug                  # bug | story | task
tracker: "${TFS_BASE_URL}/_workitems/edit/21571"   # ссылка на задачу; база — из .env
parent: 21440              # родительская US (для bug) — источник постановки
fix_task:                  # id задачи-фикса в TFS, появляется на push-pr
branch: bugfix/21571-klapan-mirror
repos: [front]             # back | front | agent — где реально идут правки
stage: implement           # текущий этап
stages_done: [intake, analyze, spec, plan]
skipped: []                # [{stage, reason}]
open:                      # незакрытые пункты — попадают в шапку сессии
  - "радиус дуги: значение в Delphi не найдено, спросить"
started: 2026-07-25T10:12
updated: 2026-07-25T12:30
---
```

`stage`, `stages_done`, `open` обновляются **на каждом этапе** — их читает hook шапки
(`hooks/preamble.mjs`) и гейт на commit.

## Секции: append-only

Одна секция на каждый заход на этап. Заголовок:
`## <этап> · итерация N · <YYYY-MM-DD HH:MM>`

Подпункты (жирным, свободный текст или список):

| Подпункт | Смысл | Обязателен |
|---|---|---|
| **что** | что делалось/сделано на этом этапе | да |
| **зачем** | какую цель это закрывает (связь с постановкой) | да |
| **почему** | почему выбран этот вариант, а не другой | если был выбор |
| **как** | способ: файлы, ссылки на Delphi (`<модуль>.pas:NNNN`), команды | да |
| **сделано** | результат, проверяемый факт | да |
| **осталось** | что не доделано в этом заходе | да |
| **нужно** | что требуется извне: ответ человека, данные, доступ | если есть |

**Прошлые секции неприкосновенны.** Повторный заход на этап → новая секция с итерацией N+1,
а не правка старой. История итераций — ценность журнала: по ней видно, где решение
разворачивалось и почему.

Исключение: опечатку в только что написанной секции править можно.

## Правила записи

1. Писать **в конце этапа**, а не «потом» — потом контекст уже потерян.
2. Ссылки, а не пересказ: `<модуль>.pas:5950`, `<репозиторий>/src/…:120`, номер PR, id
   родительской US. Журнал должен быть перепроверяемым.
3. Отрицательный результат — тоже запись: «проверил гипотезу X, не подтвердилась, потому что…».
   Это то, что чаще всего спасает следующую задачу.
4. Если данные добыты прямым поиском, потому что KB-сервер был недоступен — отметить.
5. Не дублировать в журнал код целиком: файл + строки + суть правки.

## Чек-пойнт

`/volna:checkpoint` (и перед любым `/compact`): дописать журнал так, чтобы выполнялся
инвариант восстановления. Проверка перед сжатием — три вопроса:
- по журналу понятно, **на чём именно остановились**?
- перечислены ли **следующие шаги** в `осталось`?
- все ли **открытые вопросы** в `open[]` frontmatter?

Нет хотя бы одного — дописать до `/compact`.

## Извлечение опыта (этап `capture`)

Из журнала вынимается то, что переживёт задачу, и кладётся в `.volna/knowledge/<зона>/<slug>.md`:
приём/грабли/особенность эталона/подвох инфраструктуры — **обезличенно от номера задачи**,
с одной ссылкой на источник. Что не выносится: разовые обстоятельства, детали конкретного
заказа, переписка.

Формат записи знания:

```markdown
# <короткая формулировка приёма или грабли>

**зона:** delphi-port (скилл или предметная область; совпадает с каталогом)
**этапы:** analyze, plan (где запись пригодится)
**контекст:** когда это встречается
**суть:** что делать / чего избегать
**проверено:** TASK-21571 (одна ссылка достаточно)
```

Раскладка: **одна запись — один файл** `knowledge/<зона>/<slug>.md` на 10-15 строк, рядом
`knowledge/INDEX.md` — таблицы `запись | зона | файл`, сгруппированные по этапам. Этап читает свою
секцию указателя и открывает названные записи. Файл-на-запись, а не файл-на-тему: читается файл
целиком, поэтому крупная тема тянет в контекст весь накопленный по ней опыт.

Дубликат — дописать/уточнить существующий файл, а не создавать второй.
