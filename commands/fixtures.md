---
description: Этап 7/13 · оффлайн-снимок заказа вместо живой БД (optional)
argument-hint: [что снимать, если не очевидно]
disable-model-invocation: true
---

Перейди к этапу **fixtures** активной задачи.

1. Прочитай `skills/volna-flow/stages/fixtures.md` — **только этот файл**, не весь набор — и
   выполняй его.
2. Секция `fixtures` в логе журнала уже есть → это **новая итерация** (пере-снятие снимка), а не
   переписывание прошлой: правила в скилле `volna-flow`, раздел «Повторный вызов этапа».
3. Обнови `stage` во frontmatter журнала и отметку в трекере задач сессии, в конце этапа допиши
   секцию в лог и перепиши `## Состояние`.

Этап уровня **optional**: снимок нужен не в каждой задаче. Не нужен — скажи это одной строкой
и иди дальше, причину в журнал писать не обязательно. «Дальше» значит **сразу `unit-tests`, в том
же ходе** — команды на него не жди. Останов — если снятие снимка оказалось отдельной работой
(нужен доступ к БД, отладка дампера): это разговор с человеком.

Нет каталога `.volna/` в корне этого репозитория — «Волна» здесь не развёрнута: скажи об этом одной строкой, предложи `/volna:init` и ничего не создавай.
