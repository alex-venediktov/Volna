---
description: Этап 13/13 · опыт в .volna/wiki/, замечания к флоу в .volna/feedback.md, завершение задачи (expected)
argument-hint: [что именно стоит запомнить]
disable-model-invocation: true
---

Перейди к последнему этапу **capture** активной задачи.

1. Прочитай `skills/volna-flow/stages/capture.md` — **только этот файл**, не весь набор — и
   выполняй его.
2. Обнови `stage` во frontmatter журнала и отметку в трекере задач сессии, в конце этапа допиши
   секцию в лог и перепиши `## Состояние`.

Этап снимает активную задачу из `.volna/state.json`. Новая запись знания без строки в
указателе мертва — этапы её не найдут, поэтому указатели пересобираются инструментом. Два разных
выхода: знание о продукте — в `.volna/wiki/` (остаётся в проекте), замечание о самом флоу «Волны» — в `.volna/feedback.md`
(разбирается при выпуске версии плагина); замечание, сказанное только в чате, не сохранилось.
Последняя строка ответа — `/clear`; номер следующей задачи человек назовёт уже в чистом контексте.

Нет каталога `.volna/` в корне этого репозитория — «Волна» здесь не развёрнута: скажи об этом одной строкой, предложи `/volna:init` и ничего не создавай.
