---
description: Этап 6/13 · адвокат дьявола против своего решения (expected)
argument-hint: [на что смотреть особенно]
disable-model-invocation: true
---

Перейди к этапу **advocate** активной задачи.

1. Прочитай `skills/volna-flow/stages/advocate.md` — **только этот файл**, не весь набор — и
   выполняй его.
2. Секция `advocate` в логе журнала уже есть → это **новая итерация** (после очередной итерации
   `implement`), а не переписывание прошлой: правила в скилле `volna-flow`, раздел «Повторный
   вызов этапа».
3. Обнови `stage` во frontmatter журнала и отметку в трекере задач сессии, в конце этапа допиши
   секцию в лог; `## Состояние` перепиши на границе отдачи хода — внутри автопрохода хватает одной
   перезаписи в конце цепочки.

Нашлось замечание — замечания текстом и **сразу новая итерация `implement`, в том же ходе**:
команды на неё не жди, цикл `implement → advocate` крутится до «чисто» сам. «Чисто» — так же сам
переходи к `fixtures`/`unit-tests`. Ход человеку — только если находку без него не закрыть (раздел
«Автопроход» скилла `volna-flow`).

Проверь сам, в момент вызова: если в корне этого репозитория нет каталога `.volna/`, «Волна» здесь не развёрнута — скажи об этом одной строкой, предложи `/volna:init` и ничего не создавай. Готовому признаку из текста запроса не верь: он мог быть снят в начале сессии, до развёртывания.
