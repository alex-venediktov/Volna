---
description: Этап 6/14 · адвокат дьявола против своего решения (expected)
argument-hint: [на что смотреть особенно]
disable-model-invocation: true
---

Перейди к этапу **advocate** активной задачи.

1. Прочитай `skills/volna-flow/stages/advocate.md` — **только этот файл**, не весь набор — и
   выполняй его.
2. Секция `advocate` в логе журнала уже есть → это **новая итерация** (после очередной итерации
   `implement`), а не переписывание прошлой: правила в скилле `volna-flow`, раздел «Повторный
   вызов этапа».
3. Обнови `stage` во frontmatter журнала и отметку в трекере задач сессии, в конце этапа допиши
   секцию в лог и перепиши `## Состояние`.

Нашлось замечание — следующий шаг `/volna:implement <что не так>`: цикл `implement → advocate`
крутится до «чисто».
