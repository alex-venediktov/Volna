---
description: Этап 2/14 · разбор постановки, эталона и похожих фиксов (expected)
argument-hint: [что уточнить, если этап уже пройден]
disable-model-invocation: true
---

Перейди к этапу **analyze** активной задачи.

1. Прочитай `skills/volna-flow/stages/analyze.md` — **только этот файл**, не весь набор — и
   выполняй его.
2. Секция `analyze` в логе журнала уже есть → это **новая итерация**, а не переписывание
   прошлой: правила в скилле `volna-flow`, раздел «Повторный вызов этапа». `$ARGUMENTS` —
   причина возврата; пусто — спроси её.
3. Обнови `stage` во frontmatter журнала и отметку в трекере задач сессии, в конце этапа допиши
   секцию в лог и перепиши `## Состояние`.
