---
description: Проверить настройки «Волны»: .env, .volna/project.md, доступы, гигиена git
---

Проверь готовность окружения и выведи **таблицу** «что проверено / результат / что сделать».
Ничего не исправляй молча: только сообщай и предлагай.

**Сначала прочитай профиль проекта** — секцию `## Профиль` в `.volna/project.md` — и назови его
строкой до таблицы (`один репозиторий · без трекера · commit+push · закрытие в журнал · без
эталона`). Профиль решает, что вообще проверять: строки `трекер: нет`, `репозитории: один`,
`эталон: нет` снимают проверки трекера, путей репозиториев и эталона — они выводятся как
**«не требуется в этом профиле»**, а не как «не заполнено». Пять красных строк про трекер в
проекте без трекера — шум, из-за которого перестают читать и остальные.

Три состояния профиля различай явно, это первая строка вывода:

- **заполнен** — проверяй по нему;
- **строки с плейсхолдерами** (`трекер: <нет|tfs>`) — **красная строка**: профиль не заполнен,
  первый же `intake` остановится и будет спрашивать. Назови незаполненные строки поимённо и
  предложи `/volna:init` (он спросит и запишет). Остальные проверки в этом случае веди
  по-старому, но не выдавай их за проверенные по профилю;
- **секции нет вовсе** — проект заведён до профиля: скажи это одной строкой, предложи дописать
  секцию из `templates/project.template.md` и проверяй по-старому. Это не ошибка.

**Режим текущей задачи** тоже смотри: активная задача с `mode: local` снимает трекерные
проверки (2 в части `TFS_*`, 5a, 5b, 5c) так же, как профиль без трекера.

1. **Файлы.** Есть ли `.volna/project.md`, `.env`, `.volna/journal/`, вика выводов (каталог из
   `SCHEMA.md`, по умолчанию `.volna/wiki/`), `.volna/feedback.md`. Нет — предложи `/volna:init`.
   Проверь заодно, что `.volna/` лежит **в корне репозитория** (в том же каталоге, что `.git`):
   поиск идёт вверх только до корня, поэтому проектная часть в подкаталоге не найдётся из
   остальных каталогов проекта, а hooks будут молчать без видимой причины.
1a. **Вика цела.** Не сравнивай указатель с файлами вручную — это делает линт, и точнее:
   `node "${CLAUDE_PLUGIN_ROOT}/bin/volna-wiki.mjs" lint`. Разбирать находки здесь не нужно,
   достаточно назвать сводку и предложить `/volna:lint`. Ноль ошибок при ненулевых
   предупреждениях — нормальное состояние живой вики, красной строкой это не подавать.
1b. **Замечания к флоу не залежались.** Есть ли `.volna/feedback.md` и сколько пунктов в секции
   «Открытые» (считай строки, тела не пересказывай). Файла нет — скажи, что этапу `capture`
   писать некуда, и предложи создать его (`/volna:init` доразвернёт проект, заведённый до
   появления правила). Пунктов больше пяти — отметь: их пора разобрать и перенести в плагин,
   иначе файл превращается в свалку.
2. **Переменные `.env`** — только те, которых требует профиль:
   - `TARGET_BRANCH` (прежнее имя `TFS_TARGET_BRANCH`) — нужна везде, где доставка доходит до
     push или PR. Пуста → назвать это **обязательным для доставки**: `pr create` без неё требует
     явный `--target`, и отказ всплывает в середине `deliver`, когда коммит и push уже сделаны.
     Проверь заодно, что ветка с таким именем в репозитории есть
     (`git ls-remote --heads origin <ветка>` либо `git branch -r`): опечатка даёт тот же отказ,
     но выглядит как заполненная настройка;
   - `трекер: tfs` → `TFS_BASE_URL`, `TFS_PROJECT`, `TFS_PAT_FILE`. Для `TFS_PAT_FILE` —
     существует ли файл и непустой ли он; **значение токена не выводи и не логируй**;
   - `репозитории: несколько` → все переменные `REPO_*`, названные таблицей в `.volna/project.md`
     (жёсткого списка имён нет: роли задаёт проект). Профиль `один` — переменных быть не должно
     вовсе, работа идёт в каталоге с `.volna`;
   - `эталон: delphi` → `REF_ROOT` (прежнее имя `DELPHI_REF_ROOT`).

   Для всех путей — существуют ли они на диске.
3. **Гигиена секретов.** Убедись, что `.env`, `.volna/state.json` и `.volna/feedback.md`
   попадают в `.gitignore` (`git check-ignore -v .env`). Не попадают — это первое, что надо
   починить: в проекте, развёрнутом до появления файла замечаний, правила на него может не быть,
   и замечания о процессе уедут в коммит.
   Проверь также, что `.env` не находится в индексе (`git ls-files --error-unmatch .env`);
   если он там — скажи прямо, что секрет уже под версионным контролем.
4. **`.volna/project.md`.** Остались ли незаполненные `<заполнить>`-плейсхолдеры.
5. **База знаний.** Доступен ли MCP-сервер из `KB_MCP_NAME` (есть ли инструменты `search_*`).
   Недоступен — не ошибка: отметь, что поиск пойдёт через `Grep`/`Glob`.
5a. **Доступ к трекеру.** `node <плагин>/bin/volna-tfs.mjs check` — читающая команда, ничего не
   меняет. Отличай по ответу: 401 (PAT истёк, не тот scope, BOM в файле), 403 (прав нет),
   status 0 (сеть или VPN). Значение токена не выводи.
5b. **Исполнитель новых задач.** `volna-tfs whoami` — на кого уйдёт задача-фикс на `deliver`.
   Команда не назвала пользователя (PAT сервисной учётной записи, пустой ответ) — отметь: задачи
   будут создаваться без исполнителя, и на `deliver` нужен явный `--assign`.
5c. **Поля процесса.** `volna-tfs fields Task` — какие поля обязательны у новой задачи. Есть
   обязательные, значение которых постоянно для области, и в вики о них записи нет
   → предложи завести её в `wiki/process/tfs/`: иначе
   создание задачи на `deliver` каждый раз падает на `TF401320`.
6. **Состояние задачи.** Есть ли активная задача в `.volna/state.json` и сходится ли она с
   текущей веткой git. Расхождение — предупреди: это признак смешивания задач. Локальная задача
   (`mode: local`) ветки может не иметь вовсе — тогда сверять нечего, и это не расхождение.
   Проверь заодно, что файл состояния активной задачи существует: `state.json` указывает на
   `TASK-<id>.md`, которого нет → шапка «Волны» молчит, и работа идёт без сопровождения.
6a. **Гигиена журнала активной задачи** — читай `TASK-<id>.md` целиком, из лога `logs/TASK-<id>.log.md`
   бери только заголовки секций (`Grep` по `^## `):
   - секции `## Состояние` нет → восстановление после `/compact` пойдёт по логу целиком;
   - её метка старше последней секции лога → резюме отстало и вводит в заблуждение;
   - в `open[]` больше пяти пунктов или есть формулировки в прошедшем времени («сделано»,
     «перенесено», «снято») → это не открытые вопросы, им место в «Состоянии»;
   - секция «Состояние» **больше экрана** (ориентир 6 КБ) или содержит подзаголовки `###` →
     назвать размер и предложить ужать: «где что лежит и как запускается» и метод — в
     `.volna/wiki/`, история прогонов — в лог, список дефектов — в артефакт задачи;
   - подпункты названы **не по шаблону** (`**цель задачи:**` вместо `**цель:**`, `### Следующий
     шаг` вместо `**следующий шаг:**`) → сказать прямо, что hooks и следующая сессия их не найдут,
     и назвать недостающие имена;
   - секции этапов лежат в `TASK-<id>.md` (журнал начат до разделения на два файла) → это рабочий
     формат, не ошибка; сказать одной строкой, что лог можно отделить, и не настаивать.
   В остальных случаях предложить `/volna:checkpoint`.
7. **Рабочее дерево.** `git status --short` в каждом репозитории профиля (при
   `репозитории: один` — в текущем): чисто или нет.

7а. **Вики выводов.** Если каталог вики есть, прогони
   `node "${CLAUDE_PLUGIN_ROOT}/bin/volna-wiki.mjs" stats` и приведи строкой: записей, разделы,
   типы, доля со сверкой, файлы сверх порога, шардированы ли указатели. Дальше три проверки:

   - **границы git.** Вики коммитится и шарится команде, журналы и `feedback.md` — нет. Проверь
     `git check-ignore -q` на каталог вики (не должен игнорироваться) и на `.volna/journal/`
     (должен). Вынесённая наружу вики особенно этого требует: правила игнора при переносе
     не переезжают;
   - **пороги.** Файл сверх мягкого порога строк или шардирование, которое пора включить, — это
     рекомендация, а не ошибка: назови и предложи, сам не дроби;
   - **давность сверки.** Доля записей без отметки `**проверено:**` — если она заметна,
     предложи `/volna:verify`.

   Каталога вики нет — одна строка «вики не развёрнута» и предложение
   `volna-wiki init`; ничего не создавай.

8. **Сверка скиллов: пересечения зон.** Отдельный раздел вывода, но **не при каждом прогоне**:
   пересечения не заводятся сами между двумя проверками. Делать его, если проект заводится
   впервые, если в `~/.claude/skills` или `.claude/skills` появились новые скиллы (сравни с тем,
   что видел прошлый прогон) либо человек попросил явно. Тогда — читай
   `skills/volna-flow/references/skill-audit.md` плагина и работай по нему: инвентарь трёх
   источников, сопоставление по зонам, вердикт четырёх видов на каждое пересечение. Скиллов ни
   личных, ни проектных нет — так и скажи одной строкой, файл не открывай.

В конце — одна строка вывода: можно начинать задачу или сначала что-то дозаполнить.
