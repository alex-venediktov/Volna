---
description: Этап 12/13 · уборка: репозитории, слитые ветки, индекс базы знаний (expected)
argument-hint: [что убрать особо]
disable-model-invocation: true
---

Перейди к этапу **cleanup** активной задачи.

1. Прочитай `skills/volna-flow/stages/cleanup.md` — **только этот файл**, не весь набор — и
   выполняй его.
2. Обнови `stage` во frontmatter журнала и отметку в трекере задач сессии, в конце этапа допиши
   секцию в лог и перепиши `## Состояние`.

Действия локальные, но заметные: **по подтверждению человека и по одному**. Ветку этой задачи не
удалять, пока её PR не слит. Дальше — последний этап `/volna:capture`.

Нет каталога `.volna/` в корне этого репозитория — «Волна» здесь не развёрнута: скажи об этом одной строкой, предложи `/volna:init` и ничего не создавай.
