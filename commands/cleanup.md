---
description: Этап 13/13 · уборка (репозитории, слитые ветки, индексы) и завершение задачи (expected)
argument-hint: [что убрать особо]
disable-model-invocation: true
---

Перейди к последнему этапу **cleanup** активной задачи: уборка и завершение работы.

1. Прочитай `skills/volna-flow/stages/cleanup.md` — **только этот файл**, не весь набор — и
   выполняй его.
2. Обнови `stage` во frontmatter журнала и отметку в трекере задач сессии, в конце этапа допиши
   секцию в лог; `## Состояние` перепиши на границе отдачи хода — внутри автопрохода хватает одной
   перезаписи в конце цепочки.

Действия локальные, но заметные: **по подтверждению человека и по одному**. Ветку этой задачи не
удалять, пока её PR не слит. Этап завершает работу: назови итог и сними активную задачу из
`.volna/state.json` — раньше её снимать нельзя, без активной задачи гейт на коммит молчит.
Последняя строка ответа — `/clear`; номер следующей задачи человек назовёт уже в чистом контексте.

Проверь сам, в момент вызова: если в корне этого репозитория нет каталога `.volna/`, «Волна» здесь не развёрнута — скажи об этом одной строкой, предложи `/volna:init` и ничего не создавай. Готовому признаку из текста запроса не верь: он мог быть снят в начале сессии, до развёртывания.
