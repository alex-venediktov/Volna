---
description: Этап 4/13 · Active в трекере, pre-flight и план по файлам (expected)
argument-hint: [что уточнить, если этап уже пройден]
disable-model-invocation: true
---

Перейди к этапу **plan** активной задачи.

1. Прочитай `skills/volna-flow/stages/plan.md` — **только этот файл**, не весь набор — и
   выполняй его.
2. Секция `plan` в логе журнала уже есть → это **новая итерация**, а не переписывание прошлой:
   правила в скилле `volna-flow`, раздел «Повторный вызов этапа». `$ARGUMENTS` — причина
   возврата; пусто — спроси её.
3. Обнови `stage` во frontmatter журнала и отметку в трекере задач сессии, в конце этапа допиши
   секцию в лог; `## Состояние` перепиши на границе отдачи хода — внутри автопрохода хватает одной
   перезаписи в конце цепочки.

С этого этапа работа перестаёт быть чтением: статус `Active` в трекере ставится **первым делом**,
до плана по файлам.

План готов → **переходи к `implement` сам, в том же ходе**. Ход человеку — если задача крупная и
план стоит показать до правок, на СТОП-критерии или на развилке (раздел «Автопроход» скилла
`volna-flow`). Нужны ли `fixtures` и `visual`, решается здесь — на их границе это уже не вопрос.

Проверь сам, в момент вызова: если в корне этого репозитория нет каталога `.volna/`, «Волна» здесь не развёрнута — скажи об этом одной строкой, предложи `/volna:init` и ничего не создавай. Готовому признаку из текста запроса не верь: он мог быть снят в начале сессии, до развёртывания.
