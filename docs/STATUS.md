# Волна — статус работ

> Точка входа для новой сессии: что сделано, какие решения приняты, что осталось.
> Обновлять в конце каждой сессии.
>
> Управляющий промпт-пакет (`docs/PROMPT-volna-system.md`) — внутренний рабочий документ,
> он **не в репозитории** (в `.gitignore`), лежит только локально. Всё, что нужно для работы,
> продублировано здесь.

**Последнее обновление:** 13.09.2026 (версия плагина 0.1.54: перенято семь практик из разбора
`deepseek-harness` - свидетельства и замок чек-пойнта, тип записи «постмортем», линт документации
`/volna:doc-lint`, фаза задачи и разоружённый возобновлением автопроход, скилл `prose-standard`,
строка «Не делает» у команд, корпус решений вынесен в [`DECISIONS.md`](DECISIONS.md) - решение 61;
разбор чужого агентского харнеса - см. раздел «Исследование: агентский харнес DeepSeek»;
11.09.2026, версия плагина 0.1.53: `recall` дописывает кандидата в
подпункт «в вики», когда видит расхождение записей, отставшую отметку `**проверено:**` или
промах указателя - решение 60;
0.1.52: локатор вики разбирается независимо
от пунктуации, неразобранный называется вслух; метка времени берётся из ответа часов до сборки
текста; разобраны накопленные замечания к флоу - решение 59;
0.1.51: крупная работа делится на части
внутри одной задачи - решение 57;
0.1.50: отрицательный ответ трекера
перепроверяется доступом - решение 56;
0.1.49: поддержан второй трекер, Jira Cloud, и разобраны
накопленные замечания к флоу - решения 54 и 55;
0.1.48: специфика трекера вынесена из текстов
этапов в файлы по профилю - решение 53;
0.1.47: детекторы разведки называют свой шум и
способ отсева, формы измерены на живом корпусе - решение 52;
0.1.46: инструмент называет выключенную сверку
якорей, неопознанный ключ `state.json` и настоящую ось шардирования - решение 51;
0.1.45: `route` находит запись по основе
слова, а не по написанной словоформе; отсев хвоста относителен; закрыты три правила, расходившиеся
сами с собой - решение 50;
0.1.44: автопроход продлён до `capture`,
хвосты `visual` и `unit-tests` больше не ведут мимо сбора знания - решение 49;
0.1.43: `capture` переехал перед `deliver` -
знание уезжает тем же коммитом, завершение задачи ушло в `cleanup`, разобраны три замечания
из `feedback.md` - решение 48; 0.1.42: `/volna:wiki-survey` - разведка кода
под первичное наполнение вики, зона вики под префиксом `wiki-` - решение 47; 0.1.41: собственная
вика «Волны» пополнена
выводами о раскладке и указателях - четыре записи в зоне `volna/wiki`; 0.1.40: вика выпрямлена - узлы переехали
из каталогов в имена файлов, указатели легли в корень раздела, порог указателя стал байтовым -
решение 46; 0.1.39: указатель стал деревом узлов,
добавлены `route` и `place` - решение 45; 0.1.38: указатель раздела больше не рвёт
короткую секцию на таблицы по подкаталогам - решение 44; 0.1.37: флоу переведён с knowledge на вику -
решение 43; 0.1.36: три правки механики вики по итогам
живого переезда — решение 42; 0.1.35: механика вики выводов —
решение 42; 0.1.34: разобраны четыре замечания
из `.volna/feedback.md` — решение 41; 0.1.33: сверка скиллов вынесена из `doctor` — решение 40;
0.1.32: тело `volna-journal` разгружено вдвое — решение 39; 0.1.31: пустое поле frontmatter — строка,
решение 38; 0.1.30: профиль проекта и граница
репозитория — решения 36–37; 0.1.29: состояние закрытой задачи — архив,
решение 35; 0.1.28: hooks следят за размером и форматом «Состояния» — решение 34; 0.1.26–0.1.27:
журнал разделён на состояние и лог, логи в `journal/logs/` — решения 32–33; 0.1.25: локальный
режим — решение 31; 0.1.24: шесть замечаний из `.volna/feedback.md` — решения 25–30)
**Текущее состояние: W1–W4 закрыты.** Опубликовано: `github.com/alex-venediktov/Volna`,
версия закреплена тегом **`v0.1.52`** (прежние срезы — от `v0.1.5` до `v0.1.51`).
W1 проверена на живой задаче (замечания влиты, PR #1);
**W2 и W3 проверены в живой работе 26.07 — нареканий нет**, тесты 60 и 179 проверок проходят;
W4 — `docs/ONBOARDING.md`, `docs/COST.md` с замерами от 26.07, раздел ограничений в `README.md`.

Дальше — не следующая волна, а накопление опыта на задачах и исследование подключаемого
коннектора внешнего канала для вопросов (см. «Что осталось»).

---

## Архитектурные решения (основа всего остального)

1. **«Волна» не может быть MCP-сервером.** MCP пассивен: не инжектирует промпты, не видит
   событий сессии и заполнения контекста. Поэтому разделение: флоу → skills + команды + hooks;
   состояние → markdown-файлы; интеграции и семантический поиск → MCP/CLI на периферии.
2. **Автоинъекция контекста — только hooks** (`UserPromptSubmit`). CLAUDE.md статичен, скиллы
   вызываются по решению модели, MCP-промпты — человеком. Шапка сессии — жёсткий бюджет
   ≤20 строк, иначе она × каждое сообщение съедает больше, чем экономит.
3. **Три уровня строгости** вместо «обязательно/необязательно»: `required` (гейт hook'ом,
   только необратимое), `expected` (напоминание, пропуск требует причины в журнал),
   `optional` (упоминание). По умолчанию — `expected`.
4. **Источник правды о задаче — журнал**: `.volna/journal/TASK-<id>.md` (YAML-frontmatter и
   секция «Состояние») плюс `.volna/journal/logs/TASK-<id>.log.md` (append-only лог итераций).
   `state.json` — только указатель на активную задачу. Дублировать состояние в двух местах
   нельзя: разойдётся.
5. **Дешёвый контекст как требование.** Один прогретый контекст вместо перезапусков процесса
   (headless-рестарт заново оплачивает системный промпт, конвенции и tool-схемы),
   `SKILL.md` ≤100 строк с деталями в отдельных файлах, KB-поиск вместо чтения файлов целиком,
   `/compact` после чек-пойнта вместо рестарта.
6. **Отвергнуто сознательно:** оркестратор, worktree на задачу, параллельные задачи, свой
   web-UI, автоматический сторож контекста по порогам, вендор-нейтральный слой требований,
   полноценный таймшит. Это противоречит идее одной сессии и дешёвого контекста.

---

## Что сделано

### W1 — флоу и журнал ✅ проверено на живой задаче

| Артефакт | Что внутри |
|---|---|
| `skills/volna-flow/SKILL.md` | карта 13 этапов (хвост — `capture` → `deliver` → `close` → `cleanup`), автопроход intake → capture, шкала required/expected/optional, ветвление bug/story/task, пропуск и повторная итерация, гигиена контекста; детали — `references/autopass.md`, `references/duties.md` |
| `skills/volna-flow/stages/*.md` (13) | вход · что делать · DoD · что в журнал · типичные ошибки |
| `skills/volna-journal/SKILL.md` | frontmatter как состояние, append-only секции, инвариант восстановления, чек-пойнт, формат записи знания |
| `skills/volna-*/templates/*` (4) | журнал задачи, настройки проекта, указатель знаний, замечания к флоу |
| `commands/*.md` (22) | по одной на этап: task, analyze, spec, plan, implement, advocate, fixtures, unit-tests, visual, deliver, close, cleanup, capture; служебные: init, doctor, status, skip, journal, checkpoint, recall, off, on |

**Влито по итогам прогона (PR #1):**

- **знание = один файл в каталоге зоны** (`knowledge/<зона>/<slug>.md`, 10–15 строк) вместо
  файла-на-тему: читается файл целиком, поэтому крупная тема тянула в контекст весь накопленный
  по ней опыт;
- **указатель `knowledge/INDEX.md`** с секциями по этапам — этап читает свою секцию и открывает
  названные записи, каталог целиком не читает;
- в `plan` — требование установить **компонент, который реально рендерит экран** (по импортам и
  роутеру, а не по совпадению имени файла с названием экрана);
- визуальная сверка — с работой без эталонного изображения.

Доведено следом (иначе влитое не работало бы у нового человека): шаблон
`knowledge-index.template.md`, создание `INDEX.md` в `install.ps1` и `/volna:init`, чтение
секции указателя на `plan`, `implement`, `unit-tests`, проверка расхождения указателя с файлами
в `/volna:doctor`.

### W2 — hooks ✅ проверена в живой работе

| Файл | Событие | Что делает | Блокирует |
|---|---|---|---|
| `hooks/session-start.mjs` | `SessionStart` | задача, этап, открытые пункты, возраст журнала, текущее время машины | нет |
| `hooks/preamble.mjs` | `UserPromptSubmit` | шапка ≤20 строк: задача, этап k/13, `сейчас <дата время>`, до 3 открытых пунктов, «журнал не дописан N мин»; id из промпта — локальный `\b\d{6}-[a-z0-9-]+\b`, ключ трекера `\b[A-Z][A-Z0-9_]*-\d+\b` или номер `\b\d{4,6}\b` — поднимает **существующий** журнал (без поиска по смыслу); из кандидатов берётся первый, у которого журнал есть | нет |
| `hooks/gate.mjs` | `PreToolUse` на `Bash` (команду отбирает сам гейт: условия по команде у `hooks.json` нет) | commit без записи журнала по текущему этапу → отказ (в любом режиме); push вне этапа доставки `deliver` → отказ, кроме `mode: local` (доставки нет); баг без `fix_task` перед push → предупреждение | **да** |
| `hooks/lib/volna-state.mjs` | — | поиск `.volna` вверх по дереву, `state.json`, мини-парсер frontmatter, чтение состояния и лога как двух файлов (с откатом на однофайловый журнал), бюджет строк | — |
| `hooks/hooks.json` | — | конфигурация через `${CLAUDE_PLUGIN_ROOT}`, таймаут 5 с | — |

Соблюдено: Node `.mjs` без зависимостей, чтение только `.volna/**`, без сети, **тихий выход 0**
при любой внутренней ошибке (единственное исключение — намеренный отказ гейта, и он оформлен
не кодом выхода, а `permissionDecision: deny` с человеческим объяснением).

`/volna:off` (`muted: true`) глушит шапку и напоминания, но **не гейт** — глушится
сопровождение, а не защита необратимого.

**Измерено:** 420–440 мс на вызов hook'а (почти всё — старт процесса Node на Windows);
в инвентаре плагина hooks помечены `harness-only — no model context cost`. Шапка при этом
платится на **каждом** сообщении: это главный кандидат на отключение, если польза не проявится
(как — в `docs/INSTALL.md`).

**Тест:** `node hooks/test-hooks.mjs .` — 86 проверок на синтетическом `.volna`: молчание без
каталога, бюджет шапки, `muted`, отказ и пропуск гейта на commit/push, безразличие к посторонним
командам, битый `state.json`, журнал без frontmatter. Все проходят.

**Прогон в живой работе (26.07.2026): нареканий нет.** Шапка видна и не мешает, гейт не
срабатывает зря. Решение по открытому вопросу: **`UserPromptSubmit` оставляем** — цена
(0.42–0.50 с и ~100 токенов на сообщение) себя оправдала.

**Hook на `PreCompact` удалён (27.07.2026).** Он затевался, чтобы журнал дописывался до сжатия
контекста, и этой цели достичь не мог:

- схема события не принимает `hookSpecificOutput.additionalContext` — вывод отбрасывался
  целиком, hook падал с ошибкой валидации, и напоминание не доходило ни до модели, ни до
  человека (обнаружено в живой работе 27.07);
- дать модели ход этот hook в принципе не способен: сжатие идёт сразу после него, паузы на
  дозапись нет. Доступны только сообщение человеку (`systemMessage`) и отмена сжатия
  (`continue: false`), а отменять автосжатие вредно — контекст к этому моменту переполнен;
- сигнал «журнал отстал» уже даёт шапка `UserPromptSubmit` («журнал не дописан N мин»,
  «Состояние отстало от лога») — раньше по времени и **в контексте модели**.

Дозапись остаётся дисциплиной: `/volna:checkpoint` перед `/compact`. Если журналы начнут
теряться на практике, рабочее место для автоматики — hook на `Stop`: он документированно
принимает `additionalContext` как обратную связь модели, и разговор продолжается, то есть модель
успевает дописать журнал. Нужен порог по времени и защита от цикла (`stop_hook_active`).

### Журнал: резюме отдельно от лога (26.07.2026)

**Проблема из живой работы.** Журнал длинной задачи дорос до ~1400 строк и 15 секций: восстановление
контекста требовало читать лог целиком, а ранние итерации противоречили поздним. В нём же
стихийно завелась секция «Состояние задачи на 07:35» — посреди лога, устаревшая на шесть
итераций, то есть читалась как актуальная и вводила в заблуждение. Параллельно `open[]` разросся
до 14 пунктов и превратился в свалку: там лежали выполненное и снятые вопросы, а первые три
пункта уходят в шапку каждого сообщения.

**Решение — два слоя в одном файле**, а не два файла (два разошлись бы и потребовали второго
пути в hooks и `state.json`):

| Слой | Правка | Роль |
|---|---|---|
| frontmatter | перезаписывается | состояние машины; `open[]` — только ждущее человека |
| `## Состояние · <дата>` | **перезаписывается целиком** | картина сейчас: цель, установлено, решение, отвергнуто, сделано, следующий шаг, осторожно, в knowledge |
| `## <этап> · итерация N` | append-only | как к этой картине пришли |

Правила: **восстановление = frontmatter + «Состояние»**, лог открывается адресно; при
противоречии слоёв прав верхний; отменяющая итерация несёт строку `отменяет:` и обязана
отразиться в «Состоянии»; запись этапа — это две правки, а не одна.

**Миграция существующих журналов выполнена** после выпуска версии и обновления плагина: в каждый
добавлена секция «Состояние», устаревшее резюме из середины лога перенесено наверх и удалено,
`open[]` вычищен до пунктов, реально ждущих человека (в самом длинном журнале было 16 пунктов,
осталось 5). Заодно поле `tfs:` во frontmatter приведено к `tracker:` из шаблона. Секции лога не
переписывались. Порядок важен: миграция до обновления плагина рассинхронизировала бы журнал с тем,
что делает старая версия. Старые журналы без секции «Состояние» остаются читаемыми — hooks на них
только предупреждают.

Внесено: `volna-journal/SKILL.md` и шаблон журнала, `volna-flow/SKILL.md` (обязанности этапа,
гигиена контекста), `stages/intake.md`, `stages/capture.md`, команды `checkpoint`, `status`,
`journal`, `back`, `recall`, `doctor`. В hooks — `readSummary`, `summaryField`, `lastLogStamp`,
`summaryLag` в `lib/volna-state.mjs`: шапка предупреждает, если «Состояния» нет или его метка
старше последней записи лога (отставшее резюме опаснее отсутствующего); `SessionStart` печатает
«следующий шаг» из резюме; резюме упомянутого журнала берётся из «Состояния», а не из хвоста
лога. Тест `hooks/test-hooks.mjs` — **86 проверок**, проходят.

Найдено при миграции журналов (закрыто следом): подпункт «Состояния», значение которого начинается
со следующей строки (нумерованный «следующий шаг»), давал **пустой** захват - в регулярном
выражении с флагом `m` символ `$` означает конец строки, и ленивый захват удовлетворялся сразу.
`SessionStart` молчал о следующем шаге на журнале, где он как раз расписан списком. Оба тестовых
случая добавлены; синтетический журнал в тесте теперь содержит многострочные подпункты.

Найдено при проверке на живом журнале до миграции (закрыто в `v0.1.8`): заголовок старого
стихийного резюме `## Состояние задачи на <дата>` распознавался как валидная секция, причём его
метка при строковом сравнении оказывалась «новее» любой даты — предупреждение об отставании
молчало ровно в том случае, ради которого писалось. Теперь разделитель `·` в заголовке
обязателен, метка обязана читаться как дата, иначе хук просит перезаписать резюме.

### Упаковка и автономность (вне порядка волн)

| Артефакт | Что внутри |
|---|---|
| `.claude-plugin/plugin.json` + `marketplace.json` | плагины `volna` и `volna-craft`, репозиторий сам себе маркетплейс |
| `install.ps1` | установка одной командой PowerShell 5.1, без сторонних менеджеров; идемпотентна; UTF-8 **с BOM** (без BOM PS 5.1 ломает кириллицу) |
| `.env.example`, `.gitignore`, `.gitattributes` | контракт окружения; секрет — путь к файлу токена, не значение; `*.ps1` — CRLF |
| `lib/tfs-client.mjs` | клиент REST трекера: чтение, запись, вложения, HTML↔Markdown; смоук-тест на поддельном fetch |
| `docs/reference/tfs-rest.md` | подвохи API: UTF8-тело (TF401319), `$expand=relations`, HTML в многострочных полях |
| `docs/INSTALL.md`, `docs/PROJECT-SKILLS.md` | три способа установки; граница с проектными скиллами |

### Плагин `volna-craft` — ремесленные скиллы

Приватные скиллы рабочего репозитория не коммитятся, шарить их через git нельзя. Метод вынесен
в публичный плагин, обезличенный от продукта; проектные детали — в `.volna/project.md`.

| Скилл | Что внутри |
|---|---|
| `port-by-reference` | 4 правила порта (без эвристик, паритет имён, запрет реконструкции по результату, полнота), PRE-FLIGHT, СТОП-критерии; `references/cases.md` |
| `code-comments` | одна строка «что делает (откуда)», запрет номеров задач, жаргона, труднонабираемых символов |
| `commit-style` | одна строка с префиксом задачи, без трейлеров, коммит только по разрешению |
| `test-style` | имя как утверждение, обязательный doc-комментарий, для багфикса тест должен падать до правки |
| `test-fixtures` | харнес захвата не коммитится, опции записи == чтения, round-trip при захвате; `references/capture-harness.md` |
| `visual-compare` | метод сверки + рабочий `compare.py`; прогнан на синтетических данных |

**Измерено** (`claude plugin details`): `volna` — 14 компонентов, always-on ≈ **739 токенов**;
`volna-craft` — 6 скиллов, ≈ **736 токенов** (после сокращения описаний с 1 166; на вызов
скилла — 1.1–2.7k). Суммарно ≈ **1.5k** на сессию. Вход для `docs/COST.md`.

Урок: в `description` держать только зону и триггер вызова, перечисление правил — в теле.
Полный список запретов в описании стоил вдвое дороже и на выбор скилла не влиял.

`/volna:doctor` умеет **сверку скиллов**: инвентарь личных, проектных и плагинных, сопоставление
по зонам (а не по именам), вердикт на каждое пересечение — полный дубль / дубль с деталями /
конфликт правил / своя зона.

---

## Принятые решения

**Корпус решений переехал в [`DECISIONS.md`](DECISIONS.md)** - 60 пунктов, ссылки на них
по номеру («решение 57») остаются рабочими. Здесь этот файл остаётся точкой входа
в сессию: состояние работ, что сделано и что осталось.

Новое решение пишется по правилам из шапки `DECISIONS.md`: номер следующий по порядку,
состояние словом, и обязательный разбор отвергнутого.

## Что осталось

### W4 — упаковка ✅ закрыта
Плагины, маркетплейс, `install.ps1`, `docs/INSTALL.md` — готовы. Добавлено 26.07:

| Артефакт | Что внутри |
|---|---|
| `docs/ONBOARDING.md` | первая задача за 10 минут: 6 шагов от плагина до `close`, чтение отказов гейта, таблица «симптом → решение», что коммитится |
| `docs/COST.md` | только замеры 26.07 с указанием способа: always-on 773 + 736 ≈ **1.5k**, on-invoke по каждому компоненту, hooks **0.42–0.50 с** и 0 токенов, шапка **3 строки / 250 символов** на типичном журнале; отдельным списком — что оценка, а не замер, и что не измерено |
| `README.md` → «Ограничения» | сознательно отвергнутое + 9 реальных ограничений: одна задача за раз, гейт видит только `git` из сессии, один трекер, Windows-first, правила под один проект, нет семантики по журналам, журналы не шарятся, цена шапки, W2/W3 без боевого прогона |

Попутно устранены расхождения в документах: `INSTALL.md` утверждал, что hooks «появятся в W2»
и плагин их не содержит; `README.md` называл 38 проверок CLI вместо 52; `volna-flow/SKILL.md`
ссылался на гейт как на будущее.

Версия закрепляется тегом: `v0.1.5` — документы W4, `v0.1.6` — `REMOTE.md` и разведка внешнего
канала, `v0.1.7` — журнал разделён на резюме и лог, `v0.1.8` — строгое распознавание заголовка
резюме, `v0.1.9` — многострочные подпункты «Состояния» читаются целиком, **`v0.1.10` — текущая**
(создание PR и нативная связь задача-PR в CLI). Команда пинится через `"ref": "v0.1.10"` в
`.claude/settings.json`. W4 закрыта, незакрытых пунктов по волнам нет.

### Исследование: коннектор внешнего канала для вопросов ✅ разведка сделана 26.07

**Полные результаты — [`research/external-channel.md`](research/external-channel.md).**
Коротко: **своего коннектора не нужно**. Ключевая неизвестность (пункт 2 ниже) закрыта —
вернуть ответ в ждущую сессию можно штатно: **Remote Control** (ответ и подтверждение
разрешений с телефона, ноль кода, research preview) и **Channels** (MCP-сервер с capability
`claude/channel`, готовые плагины Telegram/Discord/iMessage, permission relay по `yes <id>`).
Для «Волны» остаётся необязательный hook `Notification` (матчеры `agent_needs_input`,
`permission_prompt`) с пушем на произвольный вебхук — единственная часть без preview, без Bun
и без чужих серверов.

**Решение принято 26.07:** идём путём B — **Remote Control**. Проверен на этой машине:
согласие дано, сервер регистрируется, политика организации не блокирует. Документация для
команды — [`REMOTE.md`](REMOTE.md) (как включить, пуши в `/config`, правила удалённого
подтверждения, что уходит наружу). Правило «удалённое да равно локальному» внесено в
`volna-flow/SKILL.md`.

**Путь A (hook `Notification` на свой вебхук) отложен:** при включённых мобильных пушах
Remote Control он дублируется. Вернуться к нему, если пуши через Anthropic окажутся
неприемлемы по политике данных. Путь C (Telegram-канал) — только если вопросы понадобятся
в командном чате.

**Запасной путь D — блокирующий MCP-инструмент** (`ask_human`): обычный MCP-сервер, **без
research preview, без Bun и без тумблеров Owner'а**; вопрос уходит в канал, вызов держится,
ответ возвращается результатом инструмента, транскрипт никуда не едет. Рамки: wall-clock
лимит из `timeout` сервера или `MCP_TOOL_TIMEOUT` (по умолчанию около 28 часов), idle 30 минут
для stdio (`CLAUDE_CODE_MCP_TOOL_IDLE_TIMEOUT`), с 2.1.212 вызов дольше двух минут уходит
в фоновую задачу. Цена — свой сервер на поддержке и правило «спрашивать инструментом,
а не текстом в чат». Это и есть замена Remote Control, если он не пройдёт по политике данных.

**Проверено и закрыто:** «эмулировать ответ пользователя прямой записью в stdin» —
**не работает** для интерактивной сессии: TUI держит терминал в raw-режиме, остаются только
pty-трюки (`tmux send-keys`, `TIOCSTI` — в Linux с 6.2 выключен, на Windows `AttachConsole`
плюс `WriteConsoleInput`), кроссплатформенного способа нет. Смежные варианты того же класса
(hook `Stop` с `block` и `reason`, hook `PreToolUse` с `permissionDecision`, streaming input
в headless и SDK) разобраны в файле исследования — все работают, но хуже пути D.

**Исходная задача.** Проверить, можно ли вынести ожидание ответа человека на вопрос агента во
внешний канал связи — Telegram, Slack, `ntfy.sh` — так, чтобы канал подключался как адаптер,
а не вшивался в «Волну».

**Зачем.** Сейчас вопрос агента (СТОП-критерий, подтверждение необратимого действия, выбор
между вариантами) останавливает работу до возвращения человека к терминалу. Разработчик на
совещании или в другом окне узнаёт о вопросе через полчаса. Канал в кармане превращает эти
полчаса в минуту и снимает главный аргумент против «спрашивать, а не догадываться».

**Что выяснить в первую очередь** (в этом порядке — второй пункт может закрыть задачу):

1. **Исходящее**: какая точка Claude Code даёт факт «агент ждёт ответа» — hook `Notification`,
   `Stop`, что-то ещё; отличим ли вопрос от простого завершения хода; доступен ли текст вопроса.
2. **Входящее — ключевая неизвестность**: есть ли поддерживаемый способ вернуть ответ из
   внешнего канала в **ждущую** сессию, не перезапуская процесс. Без этого остаётся
   односторонняя нотификация («тебя спросили — иди к терминалу»), и надо честно решить,
   достаточна ли она сама по себе.
3. **Форма адаптера**: hook + маленький транспорт на Node без зависимостей против MCP-сервера;
   что из этого не тянет за собой второй демон и постоянный процесс.
4. **Безопасность**: во внешний канал уходит текст вопроса, то есть фрагмент рабочего
   контекста. Что можно отправлять, что режется, как настраивается порог; токен бота — по тем
   же правилам, что PAT: путь к файлу в `.env`, не значение.
5. **Цена**: сколько стоит адаптер в always-on и во времени hook'а (мерить как в
   [`COST.md`](COST.md)); нотификация не должна повторить историю с шапкой, которая платится
   каждым сообщением.

**Рамки.** Каналы — сменные адаптеры за одним контрактом (`ntfy.sh` первым: он проще всех и
не требует бота). Никаких внешних зависимостей и второго постоянного сервиса; адреса, токены
и идентификаторы чата — только в `.env` рабочего репозитория. Это **не** шаг к оркестратору:
человек по-прежнему принимает решения, меняется только канал доставки вопроса.

**Результат исследования** — ответ «да/нет» с обоснованием и, если да, прототип на одном
канале и одном типе вопроса (подтверждение необратимого действия на `close`). Отрицательный
ответ с записанной причиной — полноценный результат.

### Исследование: агентский харнес DeepSeek ✅ разбор сделан 13.09

**Полные результаты — [`research/deepseek-harness.md`](research/deepseek-harness.md).**
Изучен [`deepseek-ai/deepseek-harness`](https://github.com/deepseek-ai/deepseek-harness)
(коммит `c291e79` от 10.09.2026, MIT, developer preview). Коротко: **устройство не переносимо,
дисциплина переносима**. `dsh` — рантайм агента на TypeScript (свой цикл, своё хранилище
сессий, микроядро «всё — плагин»); «Волна» — инструкции поверх чужого рантайма. Зато DeepSeek
решает нашу задачу — как накапливать знание о проекте в файлах так, чтобы модель его применяла, —
и местами решает лучше.

**Взять (7):** решение отдельным файлом с состоянием `предложено/принято/отклонено` и
обязательным разделом отвергнутого (разгружает этот файл: 180 КБ, 1355 строк, 60 решений);
постмортем как отдельный жанр («почему процесс пропустил», резюме на 30 секунд, обязательная
страховка); передача между частями работы из пяти полей — у нас не хватает **свидетельств**;
фаза задачи с кодом блокировки плюс правило «автопроход не переживает возобновление сессии»;
скилл `volna-craft` о дисциплине прозы (таксономия «утечки рассуждений» из восьми классов);
линт документации вне вики — битые ссылки и бюджеты объёма, с порядком «перенести, сжать,
поднять потолок»; обязательные разделы «что видит модель» и «чего не умеет» у команд и скиллов.

**Отложить (5):** советующий страж повторов hook'ом (пороги 3/5/8); спилл длинных выводов
в файл с локатором в журнале; выбор проверок по объёму правки; два флага вызываемости скилла
(модель и человек) — требует замера цены каталога; замороженный архив записей вики.

**Не брать (6):** микроядро и профили, рантайм-инварианты, workflow-скрипты и команды агентов
(решение «подагент — чтение внутри этапа» уже принято), расписания и вебхуки, полнотекстовый
индекс сессий (`/volna:recall` закрывает), двуязычные пары документов.

**Сошлись независимо:** «всё, что видит модель, восстановимо из журнала»; «неверная настройка
падает громко» (наш неопознанный ключ `state.json` при общем правиле тишины hooks); один дом
для факта; правило на пути чтения, а не в справочнике; мягкое ведение с единственным жёстким
гейтом; скиллы как процессы.

**Решения по этим пунктам не приняты** — документ предлагает, выбор за человеком; ни одна идея
в этот заход не реализована.

### Открытые вопросы
1. Границы `knowledge/`: рабочий репозиторий (сейчас так) или репозиторий «Волны» —
   переносимо между проектами, но требует чистки от проектных деталей.
2. Учёт времени: штампы этапов с суммированием при закрытии (сделано так) или запись в трекер
   по каждому этапу.
3. Архивация журналов при корпусе 20+ задач — вместе с индексом в W3.
4. История коммитов содержит первую версию `docs/PROMPT-volna-system.md` (коммит `ee5eb4c`).
   Если документ не должен быть доступен публично — нужна перезапись истории и force-push.
